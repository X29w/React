// 定义一个名为 ReactCurrentBatchConfig 的常量，类型为 React.BatchConfig
const ReactCurrentBatchConfig: React.BatchConfig = {
  // transition 属性初始化为 null，表示当前没有任何过渡配置
  transition: null,
};

// 导出 ReactCurrentBatchConfig 以便在其他模块中使用
export default ReactCurrentBatchConfig;
